1. 

	1) Flatten - перетворює вхідні данні(пікселі) на вектор величиною в розмірність цих 	пікселів, наприклад 28*28 = 784, 30*30=900(вже не підходить, бо MNIST розпізнає фото 	28*28 пікселів).

	2) Число 128 означає кількість можливих відповідей, які знайшла модель.
	
	3) 10 виходів, бо створено для MNIST 10 класів командою: Dence(10).

	4) за тим виходом, за яким буде вища оцінка, наприклад: 3 = 0.01, 4 = 0.35, 9 = 0.96, 	отже відповідь буде 9(також, оцінка лежить між 0 і 1).

2. signs(7) →  Dence(20) + Relu → Dence(5) → SoftMax.